import React from "react";

export const Title = (props) => {
  return <h1 className="title">{props.title}</h1>;
};

export const Subtitle = (props) => {
  return <h1 className="subtitle">{props.subtitle}</h1>;
};
